python-telegram-bot==21.6
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1

# v3 — transcripción local con Whisper.
# torch se instala aparte en el Dockerfile (CPU-only, más liviano).
# Sin pin de versión: que pip resuelva la última estable
# (la 20231117 tiene un setup.py incompatible con setuptools moderno).
openai-whisper
